# Optional: Set a custom path for the SQLite database file
# DATABASE_PATH=/path/to/your/docklite.db

# Optional: Set a custom path for the Docker socket
# DOCKER_SOCKET_PATH=/path/to/your/docker.sock

# Vertex AI Configuration
# Your Google Cloud Project ID (required for Vertex AI)
GOOGLE_CLOUD_PROJECT=your-project-id

# Google Cloud region for Vertex AI (e.g., us-central1, us-west1)
GOOGLE_CLOUD_LOCATION=us-central1

# Optional: API key for Vertex AI (if not using service account auth)
# GOOGLE_API_KEY=your-api-key-here

# Optional: Path to service account key JSON file
# If not set, will use Application Default Credentials (gcloud auth)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Session secret for iron-session (32+ characters recommended)
SESSION_SECRET=replace-with-a-long-random-string
